settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: app
  path: .
  args:
  - serve
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
